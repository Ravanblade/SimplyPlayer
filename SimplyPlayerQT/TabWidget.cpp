#include "TabWidget.h"


void TabWidget::paintEvent(QPaintEvent *e)
{

   /*// QPixmap widgetMask = QPixmap(this->width(), this->height());
   // widgetMask.fill(Qt::transparent);
    QPixmap mask = QPixmap(this->width(), this->height());
    mask.fill(Qt::transparent);
    QPainter p(&mask);
   // p.begin(mask);
    QFont font;
    font.fromString("Tahoma");
    p.setFont(font);
    //p.setPen(QPen(titleColor, 2))



    p.setPen(QColor(40,78,20));
    p.setBrush(Qt::SolidPattern);

    p.drawText(QRect(5, 5, 30,30), Qt::AlignVCenter, tr("W dupie"));
   // p.setBrush(backgroundColor);
   // p.setPen(QPen(titleColor, 1));


    p.end();
    this->setTabBar();
    this->tabBar()->tabButton(0,QTabBar::LeftSide)->setMask(mask.mask());*/

}
